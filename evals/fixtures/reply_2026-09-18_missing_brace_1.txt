{"action":"write_file","args":{"path":"calculator.py","content":"import tkinter as tk\nfrom tkinter import messagebox\n\n# Create the main window\nroot = tk.Tk()\nroot.title(\"Calculator\")\nroot.geometry(\"300x400\")\n\n# Variables to store the current input and result\nexpression = \"\"\nresult = 0\n\ndef clear():\n    global expression, result\n    expression = \"\"\n    result = 0\n    display.delete(0, tk.END)\n\ndef press_button(value):\n    global expression\n    expression += value\n    display.insert(0, expression)\n\ndef calculate():\n    global expression, result\n    try:\n        result = eval(expression)\n        display.delete(0, tk.END)\n        display.insert(0, str(result))\n    except:\n        messagebox.showerror(\"Error\", \"Invalid input\")\n        expression = \"\"\n        result = 0\n\ndef backspace():\n    global expression\n    expression = expression[:-1]\n    display.delete(0, tk.END)\n    display.insert(0, expression)\n\ndef close():\n    root.destroy()\n\n# Create a display\ndisplay = tk.Entry(root, font=('Arial', 16), bg='white', fg='black', width=30)\ndisplay.grid(row=0, column=0, columnspan=4, sticky=\"ew\")\n\n# Create buttons\nbuttons = [\n    \"7\", \"8\", \"9\", \"/\",\n    \"4\", \"5\", \"6\", \"*\",\n    \"1\", \"2\", \"3\", \"-\",\n    \"0\", \".\", \"=\", \"+\"\n]\n\nfor i, button in enumerate(buttons):\n    btn = tk.Button(root, text=button, font=('Arial', 16), width=5, height=2,\n                     command=lambda x=button: press_button(x))\n    btn.grid(row=(i//4)+1, column=i%4)\n\n# Add button for backspace\nbackspace_btn = tk.Button(root, text=\"<-\", font=('Arial', 12), width=5, height=2,\n                    command=backspace)\nbackspace_btn.grid(row=5, column=0, columnspan=2, sticky=\"w\")\n\n# Add button for clear\nclear_btn = tk.Button(root, text=\"C\", font=('Arial', 12), width=5, height=2,\n                    command=clear)\nclear_btn.grid(row=5, column=2, columnspan=2, sticky=\"e\")\n\n# Add button for calculate\ncalculate_btn = tk.Button(root, text=\"=\", font=('Arial', 16), width=5, height=2,\n                      command=calculate)\ncalculate_btn.grid(row=5, column=4, columnspan=2, sticky=\"e\")\n\n# Start the main loop\nroot.mainloop()"}